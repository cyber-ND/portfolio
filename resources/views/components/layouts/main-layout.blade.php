{{-- <!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Avery Davis Portfolio</title>
    @vite('resources/css/app.css')
    @livewireStyles
    @stack('styles')
</head>
<body class="min-h-screen bg-gray-900 text-white">
    <nav class="p-4 flex justify-between items-center">
        <div class="text-purple-400 text-xl">Avery Davis</div>
        <div class="space-x-4">
            <a href="{{ route('home') }}" class="text-purple-400 hover:text-pink-400">Home</a>
            <a href="{{ route('about') }}" class="text-purple-400 hover:text-pink-400">About Us</a>
            <a href="{{ route('projects') }}" class="text-purple-400 hover:text-pink-400">Projects</a>
            <a href="{{ route('contact') }}" class="text-purple-400 hover:text-pink-400">Contact Us</a>
        </div>
        <div id="profile-circle"></div> <!-- Placeholder for React ProfileCircle component -->
    </nav>
    <main class="flex-1">
        @yield('$content')
    </main>
    @livewireScripts
    @stack('scripts')
</body>
</html> --}}
